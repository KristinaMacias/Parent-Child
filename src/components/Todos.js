import React from "react";
import { list } from "./List";

export default class Todos extends React.Component {
    render() {
        return (
            console.log(list),
            <div>
                <h1>Todos</h1>
                
            </div>
        )
    }
}